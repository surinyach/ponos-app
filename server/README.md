# Ponos server

This directory is reserved for the homelab API, database models, migrations,
and backend tests.

The initial backend stack and schema will be introduced as a separate feature.
Until then, this directory intentionally contains no runtime implementation or
credentials.
